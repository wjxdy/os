# 项目待办

## 下一步
- [ ] 把 `kernel_main` 开头的串口消息改为 `Rust OS: kernel entered`，把末尾旧消息改为能准确表示双通道完成的文案。
- [ ] 复述实验 07 的生命周期、两个 `&mut` 借用、reborrow、trait 组合和部分输出失败策略，然后提交本阶段改动。
- [x] 生成实验 08“全局内核日志入口与 `print!` / `println!` 宏”的完整教学文档，参考实现已在临时副本通过格式、构建、严格 Clippy 与 QEMU 串口验证；正式内核源码仍由学习者亲手输入。
- [ ] 实验 08 中实现拥有设备的全局日志器、初始化入口、同步保护、`_print` 和两个打印宏，并从另一个模块调用它完成验收。

## 进行中
- 无。

## 待确认
- 无。

## 阻塞
- 无。

## 后续可做
- [ ] 把基础认知快速路线和两个运行故事穿插到对应内核实验，而不是作为动手前的统一门禁。
- [ ] 完成像素 framebuffer 后实现 CPU 异常与 IDT：先捕获 `int3` 和页错误，再进入外部中断与键盘。
- [ ] 第一版完成后评估 AArch64 移植。
- [ ] 基础 OS 稳定后设计用户态 AI runtime 实验。
