# 项目待办

## 下一步
- [ ] 复查实验 09/10 的正常 QEMU 启动和改动范围，提交当前尚未提交的正式源码。
- [ ] 按 `17-实验11：物理内存地图与页框分配器.md` 亲手新增 `memory.rs`，读取并打印 `BootInfo.memory_regions`。
- [ ] 实现 4 KiB 边界向内对齐、`usable_frames()` 和唯一 `BootInfoFrameAllocator`，连续分配五个 Frame 后通过格式、构建、严格 Clippy 与 QEMU 验收。

## 进行中
- 无。

## 待确认
- 无。

## 阻塞
- 无。

## 后续可做
- [ ] 把基础认知快速路线和两个运行故事穿插到对应内核实验，而不是作为动手前的统一门禁。
- [ ] 实验 11 后读取当前四级页表并理解地址翻译，再实现映射、内核堆和外部中断。
- [ ] 第一版完成后评估 AArch64 移植。
- [ ] 基础 OS 稳定后设计用户态 AI runtime 实验。
